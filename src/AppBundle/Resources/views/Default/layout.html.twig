{% extends '::base.html.twig' %}

{% block navigation %}
    {% include 'AppBundle:Default:navigation.html.twig' %}
{% endblock %}
    
{% block body %}
    {# parent() #}
    
           
    <!-- /move to external template -->
    <h1>Hello, world!</h1>
    {{ 'symfony.test'|trans({},'app') }}
    {{ 'symfony.test2'|trans }}
    <a href="{{ path('homepage') }}">test</a>
     
    <button type="button" class="btn btn-default btn-lg">{{ icon('bar-chart') }}
    {{ icon('camera') }}
    </button>
    
    
    
    
    {{ dump(translations) }}
    {{ dump(article) }}
    
    
{% endblock %}